micronaut:
  security:
    intercept-url-map:
      - pattern: /css/**
        access:
          - isAuthenticated()
      - pattern: /api/books
        access:
          - ADMIN
      - pattern: /api/**
        access:
          - isAnonymous()
      - pattern: /**
        access:
          - isAnonymous()