micronaut:
  security:
    intercept-url-map:
      - pattern: /css/**
        access:
          - isAnonymous()
      - pattern: /api/books
        access:
          - isAuthenticated()
      - pattern: /api/**
        access:
          - isAnonymous()
      - pattern: /**
        access:
          - isAnonymous()